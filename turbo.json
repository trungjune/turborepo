{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "outputs": [
        "dist/**",
        ".next/**"
      ],
      "env": [
        ".env.production.local",
        ".env.production",
        ".env.local",
        ".env"
      ]
    },
    "dev": {
      "env": [
        ".env.development.local",
        ".env.development",
        ".env.local",
        ".env"
      ]
    },
    "start": {
      "dependsOn": [
        "^build"
      ]
    },
    "test": {
      "env": [
        ".env.test.local",
        ".env.test",
        ".env.local",
        ".env"
      ]
    }
  }
}
